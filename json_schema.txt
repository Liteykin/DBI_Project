{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://example.com/user.schema.json",
    "title": "User",
    "description": "A user in the chatflow database",
    "type": "object",
    "properties": {
        "UserId": {
            "description": "The unique identifier for a user",
            "type": "integer"
        },
        "SupervisorId": {
            "description": "The unique identifier for a user's supervisor",
            "type": "integer"
        },
        "Username": {
            "description": "The username of the user",
            "type": "string"
        },
        "FirstName": {
            "description": "The first name of the user",
            "type": "string"
        },
        "LastName": {
            "description": "The last name of the user",
            "type": "string"
        },
        "Password": {
            "description": "The password of the user",
            "type": "string"
        },
        "Email": {
            "description": "The email of the user",
            "type": "string",
            "format": "email"
        },
        "BirthDate": {
            "description": "The birth date of the user",
            "type": "string",
            "format": "date"
        },
        "SignUpDate": {
            "description": "The sign up date of the user",
            "type": "string",
            "format": "date-time"
        },
    },
    "required": ["UserId", "Username", "FirstName", "LastName", "Password", "Email", "BirthDate", "SignUpDate"]
}
